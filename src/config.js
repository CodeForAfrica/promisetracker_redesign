/* Design-time switches that were props on the prototype component.
   statusPalette: 'traffic-light' | 'two-pole'
   sparseData:    render the empty-ish state (no evidence, no petitions, no analysis)
   showComingSoon: list the not-yet-launched countries on the landing page */
export const config = {
  statusPalette: 'traffic-light',
  sparseData: false,
  showComingSoon: true
};
